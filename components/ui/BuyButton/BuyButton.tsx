import React, { FC } from "react"
import styled from "styled-components"

const Container = styled.button``

const BuyButton: FC = () => {
  return <Container>Buy Beasts</Container>
}
export default BuyButton
