import React, { FC } from "react"
import styled from "styled-components"

const PackCardRevealed: FC = () => {
  return (
    <div
      style={{
        borderRadius: "12px",
        background: "#212127",
        color: "#fff",
      }}
      className="group block w-full aspect-w-9 aspect-h-7 focus-within:ring-2 focus-within:ring-offset-2 focus-within:ring-offset-gray-100 focus-within:ring-indigo-500 overflow-hidden"
    >
      test
    </div>
  )
}

export default PackCardRevealed
